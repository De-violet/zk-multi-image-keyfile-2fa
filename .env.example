# Server Configuration
PORT=3000
NODE_ENV=production

# Security & Reverse Proxy
# Diaktifkan secara otomatis di app.js untuk 1 hop proxy (Nginx / Cloudflare / Docker)
# TRUST_PROXY=1

# Database Persistence
# DB_PATH=./server/data/users.json

# Demo Mode Configuration
# ALLOW_DEMO_OVERWRITE=false
# DEMO_MODE=false
